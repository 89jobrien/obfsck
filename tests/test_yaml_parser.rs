// Tests for the build.rs YAML parser have been moved into build.rs's own
// #[cfg(test)] module, where they can call the real parser functions directly
// without duplicating helpers. See build.rs for `yaml_parser_*` and
// `validation_catches_*` tests.
//
// This file is intentionally empty. It can be deleted when convenient.
