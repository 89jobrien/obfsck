paths-ignore:
  # Synthetic test fixtures — intentionally contain fake secret patterns for redaction testing.
  # Each path is listed explicitly; do NOT use broad globs like 'tests/**'.
  - 'tests/fixtures/demo-secrets.txt'
  - 'tests/fixtures/inputs/secrets_sample.txt'
  - 'tests/fixtures/inputs/pii_sample.txt'
  - 'tests/fixtures/inputs/paranoid_sample.txt'
  - 'tests/fixtures/inputs/mixed_sample.txt'

  # Design docs that contain synthetic patterns (regex examples, fake-key code snippets).
  # Listed explicitly so new docs/plans don't get auto-suppressed.
  - 'docs/superpowers/plans/2026-03-24-visual-demo.plan.md'
  - 'docs/superpowers/specs/2026-03-23-visual-demo-design.plan.md'
